# Nostr Relay URLs (comma-separated)
VITE_NOSTR_RELAYS=wss://buildit-relay.rikki-schulte.workers.dev,wss://relay.damus.io,wss://relay.primal.net,wss://relay.nostr.band,wss://nos.lol

# BuildIt API Worker URL (link-preview, image-proxy, oembed)
VITE_API_URL=https://buildit-api.rikki-schulte.workers.dev

# Environment
VITE_ENV=development

# Optional: Tor SOCKS5 proxy (for Tor integration)
# VITE_TOR_PROXY=socks5://127.0.0.1:9050
