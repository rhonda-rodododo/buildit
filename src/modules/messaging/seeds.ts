/**
 * Messaging Module Seed Data
 * Provides example/template data for the messaging module
 */

import type { ModuleSeed } from '@/types/modules';

/**
 * Seed data for messaging module
 * Currently empty - messaging uses core database tables
 */
export const messagingSeeds: ModuleSeed[] = [];
